<view class="page">
  <view class='page-bd p-20'>
    <view wx:if="{{!nodata}}" class="section mt-1">
      <block wx:key="id" wx:for="{{myevents}}">
        <l-list title="{{item.myevent}}" bind:lintap="toMyevent">
        </l-list>
      </block>
    </view>
    <view wx:else class='nodata'>
        <image src="/img/logo_v.png"></image>
        暂无相关数据
    </view>

    <button class='d-btn-primary'  bindtap="onTap">创建赛事</button>
  </view>
</view>

<l-dialog/>
