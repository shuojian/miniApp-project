<!--components/nodata/index.wxml-->
<view wx:if="{{noType == 'list'}}" class='container'>
  <text class='desc'>{{text}}</text>
</view>
<view wx:elif="{{noType == 'page'}}" class='nodata-page'>
  <image src="/img/logo_v.png"></image>
  {{text}}
</view>
